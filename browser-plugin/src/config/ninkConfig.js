export const DEFAULT_NINK_CONFIG = {
  useDevStubs: false,
  useWalletMode: false,
  /** When true, extension calls http://127.0.0.1:8787 (packages/api). Set false for production. */
  useLocalApi: true,
};
